enum Size { small, medium, large }

enum Direction { up, down, left, right }

enum FoodType {
  vegetarian,
  meat,
  cakes,
  fish,
  desserts,
  diary,
  other,
}

enum Delivery {
  pending,
  outForDelivery,
  delivered,
  cancelled,
}

enum PaymentMethods {
  googlePay,
  creditCard,
  paytm,
  phonePay,
  uPI,
  razor,
  cashOnDelivery,
}
